# SegmentScraper v1.12.6

## Timestamp review and upload

- Show Scraper timestamps on the left and current IntroDB timestamps on the right.
- Format review times as hours, minutes and seconds, including millisecond precision.
- Omit TVDB episode details for movies.
- Keep the review screen available when IntroDB already contains the same range.
- Allow a manually approved direct IntroDB upload without requiring a JSON download first.

Validation: 216 automated tests passed, including generated userscript, desktop-core parity and direct-upload review checks.
