# SegmentScraper v1.12.4

## Userscript

- Keep online movie credit capture enabled only for Netflix while Prime Video, Videoland, SkyShowtime and other providers are being verified against playback.
- Continue capturing and exporting TV series segments for those providers.
- Remove restored non-Netflix movie captures and prevent them from reaching JSON export or IntroDB submission.

The desktop Alpha 0.1 keeps its separate, manually reviewed movie workflow. This patch also includes the desktop reliability and responsive-layout updates below.

## Desktop Alpha 0.1

- Make ending analysis follow the actual video-stream duration, so audio or metadata tails do not turn a complete scan into a false failure.
- Use available FFmpeg hardware acceleration, tolerate longer provider read stalls and reconnect transient remote-stream failures.
- Offer a reviewed-file fallback that downloads a failed provider inspection locally before retrying analysis.
- Replace the fixed-width review layout with fluid desktop columns and responsive tablet/mobile breakpoints.

Validation: 214 automated tests passed, including generated userscript, desktop-core parity and FFmpeg analysis checks.
