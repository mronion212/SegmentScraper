# SegmentScraper v1.12.5

## Userscript and Desktop Alpha 0.1

- Show current IntroDB timestamps beside the timestamps detected by the scraper for the exact item.
- Require a separate manual comparison approval before any IntroDB upload.
- Keep provider offsets, including Netflix offsets, as a manual review decision instead of autocorrecting them.
- Block malformed or undocumented IntroDB responses instead of treating them as missing data.

Validation: 216 automated tests passed, including generated userscript, desktop-core parity and IntroDB upload-review checks.
